steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/scanbatuk:$BUILD_ID', '.']
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/scanbatuk:$BUILD_ID']
  - name: 'gcr.io/cloud-builders/gcloud'
    args: ['run', 'deploy', 'scanbatuk-web',
           '--image', 'gcr.io/$PROJECT_ID/scanbatuk:$BUILD_ID',
           '--region', 'asia-southeast2',
           '--platform', 'managed',
           '--allow-unauthenticated',
           '--port', '80']
